export enum CONTRACT_STATUS {
  DRAFT = 'draft',
  TERMINATED = 'terminated',
  ACTIVE = 'active',
  EXPIRED = 'expired',
  RENEWED = 'renewed',
}

export enum CONTRACT_RENTAL_TYPE {
  DAILY = 'daily',
  MONTHLY = 'monthly',
  YEARLY = 'yearly',
}
